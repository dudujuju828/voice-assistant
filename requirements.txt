PySide6
mss
pywin32
requests
sounddevice
python-dotenv

# Optional: local (offline) TTS via Kokoro. Only needed when the TTS provider
# is set to "local"; the ElevenLabs API path works without these.
kokoro-onnx
soundfile
# For GPU acceleration, install onnxruntime-gpu instead of the onnxruntime that
# kokoro-onnx pulls in, plus the CUDA 13 / cuDNN 9 runtime wheels (NVIDIA only):
#   pip uninstall onnxruntime
#   pip install onnxruntime-gpu
#   pip install nvidia-cublas nvidia-cuda-runtime nvidia-cuda-nvrtc \
#               nvidia-cufft nvidia-curand nvidia-cusparse nvidia-cudnn-cu13
# tts_local.py auto-detects CUDA and falls back to CPU if it's unavailable.

# Optional: higher-quality local TTS with voice cloning via Chatterbox
# (Resemble AI). Only needed when the TTS provider is "chatterbox". This pulls
# in torch/torchaudio; for GPU, install the CUDA torch build FIRST so pip
# doesn't grab the CPU-only wheel:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install chatterbox-tts
# tts_chatterbox.py auto-detects CUDA (torch.cuda) and falls back to CPU. The
# ~1 GB model downloads from Hugging Face on first use.

# Optional: agentic browsing (Claude drives a Chrome window). No pip deps — it
# needs Node.js on PATH (the Playwright MCP server runs via `npx`) and Google
# Chrome installed. Enable it in Settings; see README "Agentic browsing".
